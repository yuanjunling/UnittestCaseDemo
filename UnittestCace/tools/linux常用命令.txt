sudo su //切换到root账号

查看文件内容 只读 -n显示想读的行数
cat //cat 命令：一次性显示文件的所有内容 -n 
less//less 命令：分页显示文件内容
空格键：文件内容读取下一个终端屏幕的行数，相当于前进一个屏幕（页）。很常用的快捷键。与键盘上的 PageDown（下一页）效果一样；
回车键：文件内容读取下一行，也就是前进一行，与键盘上的向下键效果是一样的；
d 键：前进半页（半个屏幕）；
b 键：后退一页，与键盘上的 PageUp（上一页）效果一样；
y 键：后退一行，与键盘上的向上键效果是一样的；
u 键：后退半页（半个屏幕）；
q 键：停止读取文件，中止 less 命令。

head 命令：显示文件开头
tail 命令：显示文件结尾
tail 命令还可以配合 -f 参数来实时追踪文件的更新：
快捷键 Ctrl + c 来终止 tail -f 命令
tail -f -s 4 syslog      #每隔 4 秒检查一次文件是否有更新
touch 命令：创建一个空白文件
mkdir：创建一个目录  -p 参数来递归地创建目录结构。mkdir -p one/two/three 就会先创建 one 这个目录，在 one 目录里面创建 two 这个目录，在 two 里面再创建 three 这个
*通配符 可以匹配任何字符串
cp 命令：拷贝文件或目录 cp -r one one_copy要拷贝目录，只要在 cp 命令之后加上 -r 或者 -R 参数（大写和小写作用是一样的，都表示 recursive，也就是“递归的”）。拷贝的时候，目录中的所有内容（子目录和文件）都会被拷贝
mv 命令：移动文件 mv 是英语 move 的缩写，表示“移动”。mv 命令有两个功能：
rm 命令：删除文件和目录 -i 参数：向用户确认是否删除 -f 参数：慎用，不会询问是否删除，强制删除 -r 参数：递归地删除 rm 命令加 -r 和 -f 参数：极为危险！

文件颜色类型：
蓝色–>目录
绿色–>可执行文件
红色–>压缩文件
浅蓝色–>链接文件
灰色–>其他文件